extends layout

block content
  .hero
    .container.hero-content
      .card
        h1 Welcome to the Health Tracker
        p.lead Jump straight to:
        .btn-group
          a.btn.btn-gradient(href="/exercise") 🏃 Exercise
          a.btn.btn-gradient(href="/diet") 🥗 Diet
          a.btn.btn-gradient(href="/group") 👥 Groups
